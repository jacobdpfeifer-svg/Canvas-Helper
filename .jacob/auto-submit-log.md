# Auto-submit log

One row per native Canvas auto-submit. Agent appends after each auto-submit (never for LTI/quiz/proctored).

| Date | Course | Assignment | Why auto | Policy source |
|------|--------|------------|----------|---------------|
| *(none yet)* | | | | |

Policy source: `synced agent_writes` from `## Syllabus / agent policy notes` or MCP `get_course_policy`.
