# calendar-suggestions.jsonl

Each line: `{"source_message_id","title","start","end","confidence","why"}`.

Written by a future Gmail triage path (`mcp-servers/gmail/`, skills). This round: reader in Calendar tab + fixture. Do not auto-add. Confirm account scope before wiring a producer.
