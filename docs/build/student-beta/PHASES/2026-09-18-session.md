# Session notes — 2026-09-18

Phase 0: baseline green except native (Rust absent → Homebrew install; iCloud EPERM → mirror script).
Phase 2: study core (model/clock/store/checkers/packets/reducer/select/service/cli), six packets, 56 tests; workspace UI; Vitest harness; browser walkthrough found the "reload hides open attempt" defect → fixed with regression.
Parallel-agent interruption: candidate B built alongside; both preserved on branches; A restored; audit-A findings reproduced and fixed (16 regressions).
Phase 3: `sync-study-sources.mjs` + `study/canvas.py` + Sources UI + authoring; fixture-tested; walkthrough. Sleep-induced clock false positive found → sleep-aware clock, no latch.
Phase 4: FirstRun (3 steps), keyboard test, contrast measurements, phone width.
Phase 5: relay service (19 tests) + client (3 tests) + UI.
Phase 6: connectors (8 tests) + Settings/Plan UI + PlanEvent test.
Phase 7: stage script, resource map, unsigned .app build, bundled-core probes, site.
Phase 8: comparison with B; adopted bridge hardening, real-core Vitest, bounded history, fee carve-out; final matrix green.
