# Full-program audit — 2026-09-15

Run per [`audit-prompt-2026-09-15.md`](./audit-prompt-2026-09-15.md) on throwaway branch `audit/2026-09-15` from `phase1-productname-pivot` @ `1bf168f`. Scope from `CLAUDE.md`, `AGENTS.md`, and `docs/architecture.md` overrides everything below. Gaps against those boundaries are **confirmed intentional** — not defects. Signed rows in [`pre-ship-decisions.md`](./pre-ship-decisions.md) are pointed at, not re-litigated.

**Not verified (needs live credentials / human):** Canvas SSO sync, PAT MCP live calls, real Gmail send / GCal create-update, Chrome Native Messaging live round-trip, two-device SSO. See human-walk rows in [`pre-ship-decisions.md`](./pre-ship-decisions.md) (#2–#4).

**Environment:** `uv` + Homebrew Node available; no `cargo`/`rustc` on PATH — `cargo test` skipped. Python 3.12 via project `.venv`.

---

## Ran

| Command | Result |
|---------|--------|
| `uv run python -m pytest tests/ -q` | **741 passed, 20 skipped.** |
| `uv run python -m pytest tests/security/test_student_write_invariants.py tests/tools/test_discussions_preview.py -q` | **53 passed** (preview-only lock). |
| `cd browser && npm run test` | **89 pass, 0 fail.** |
| `cd browser && node scripts/validate-profiles.mjs` | **Pass** — “All instructor profiles passed validation.” |
| `uv run ruff check .` | **Clean.** |
| `uv run mypy src/` | Initially **4 errors** (habit / teach_hint / claim_context). After inline fixes: **Success: no issues found in 53 source files.** |
| `cd app && npm run build` (`tsc` + `vite build`) | **Pass** (~466ms vite). App has recent commits (`1bf168f`) despite “parked” — built per prompt rule. |
| Tool-layer scan (`@mcp.tool` × `@validate_params`) | **41/41** tools have `@validate_params`. |
| Manifest ↔ source ↔ schemas | **41/41** names align across `tools/TOOL_MANIFEST.json`, `tools/TOOL_INPUT_SCHEMAS.json`, and `src/canvas_mcp/tools/`. Spot-check: `submit_assignment`, `comment_on_my_submission`, `get_my_upcoming_assignments`, `get_my_course_grades`, `list_courses` present in all three. |
| Skill router smoke (`load_skills_for_routing` + `route_skill`) | **14/14** bundled skills load; AGENTS index ↔ dirs exact. Sample routes OK after trigger fix (below). `tests/core/test_skill_router.py` **19 passed.** |
| `cargo test` (`app/src-tauri`) | **Skipped** — no Rust toolchain. Inline `#[test]` in `inbox.rs` / `daemon.rs` unexercised. |
| mcp-servers unit tests | **None present.** Covered indirectly via `tests/core/test_actuator_common.py`, `test_connector_guards.py`, `test_google_oauth_helpers.py`. |
| `python3 scripts/check_closing_keywords.py -` | Exercised via `tests/test_closing_keyword_guard.py` (suite included above); not a bare offline linter. |

**Skipped deliberately (side effects / credentials):** `npm run sync`, `open-canvas`, live CampusGroups RSVP, Gmail/GCal OAuth execute, PAT `canvas-mcp-server --test`, root `package.json` `tsc` over upstream code-execution API (student registry excludes `execute_typescript` — confirmed by `test_role_filtering`).

**Pass 2 wire checks executed (not separate “commands”):**

- SKILL.md relative links: 61 checked; 17 user_root-style (`../../USER.md`, `../../calibration/*`) expected-missing in-repo; 2 “broken” hits were fenced-template placeholders `[source title](url)` inside `student-concept-visual` output example — false positive, not fixed.
- Plugin registry: single entry `cu-boulder/campusgroups`; `pluginDir` + `shim` paths exist; no orphan plugin dirs.
- Gmail `send_email` / GCal `create_event`/`update_event`: each calls `gate_connector_write` before live/dry-run write. Apple Calendar `create_event` always `blocked`.
- Connector writes: production MCP funnel is `gate_connector_write` → `get_connector_guard`; no forgeable `confirmed: bool` bypass on send/calendar.
- Fresh out-of-scope grep (`skills/`, `docs/` excl. historical handoff noise, `src/`, `examples/` if present): policy/refusal language only; `self_improve/` package **absent**; no RMP scraper; `vendor/examples/` may retain upstream educator wording (archived, not product surface).
- No `skill_route.py` duplicate (only `skill_router.py`).

---

## Fixed

Mechanical Pass 1/2 only (not committed):

1. **`src/canvas_mcp/core/habit.py`** — `brief_timezone()` missing return annotation → `-> tzinfo` (+ import).
2. **`src/canvas_mcp/core/teach_hint.py`** — `main()` reused `path: Path` then assigned `write_nudge()` → `Path | None`, false unreachable on None. Annotated `path: Path | None` for prior-knowledge; renamed nudge result to `nudge_path: Path | None`.
3. **`src/canvas_mcp/core/claim_context.py`** — explicit `professional_context is None` guard before `list(...)` in `context_for_display`.
4. **`skills/student-instructor-profile/SKILL.md`** — AGENTS phrase “how does this prof grade” did not match literal bracketed trigger `"how does [prof/course] grade"`. Added natural-language trigger bullets.
5. **`skills/canvas-discussion-facilitator/SKILL.md`** — AGENTS “discussion draft” did not route; added `discussion draft` trigger.

Verified: `mypy src/` clean; `pytest tests/` 741/20; router smoke routes the new phrases; `test_skill_router` 19 passed.

---

## Findings, not fixed

### Confirmed intentional (do not “fix”)

| Item | Evidence |
|------|----------|
| `submit_assignment` / `comment_on_my_submission` / `post_discussion_entry` / `reply_to_discussion_entry` preview-only | `readOnlyHint=True`; preview strings; no POST/execute; locked by `test_student_write_invariants` + `test_discussions_preview`. |
| No Buff Portal / DegreeWorks scraper; no registration execute | `degree_audit.py` + skills are paste-only. |
| No `self_improve` pipeline | Tree absent under `src/canvas_mcp/core/`. |
| No learning streaks / leaderboards | `habit` = brief-day continuity only; `class-standings.md` framed, not authorized. |
| Apple Calendar hard-blocked | `mcp-servers/apple-cal/server.py` always returns `blocked`; `SWIFT_HELPER` unused; no EventKit binary. |
| CampusGroups RSVP via CLI `--confirm` (not MCP ConfirmationGuard) | Documented escape hatch; registry still says `required-on-mcp-write` for any future MCP write. |
| Educator / hosted Azure / quiz automation | Out-of-scope grep: policy/refusal only on product surface. |
| Gmail `create_draft` / `apply_labels` may proceed without token when posture is `automatic` | Intentional; `email_send` / `calendar` are in `ALWAYS_GATED` and cannot escalate. |
| Gmail/GCal `rewind` without a fresh gate | Intentional undo of prior ledger writes. |
| Tauri shell replacement / packaging | Point at [`pre-ship-decisions.md`](./pre-ship-decisions.md) rows 3–4 (signed park / not yet). |

### Wiring / docs / env (judgment or non-trivial)

1. **`app/` “Parked” vs post-park feature landings** — architecture L29 + pre-ship decision #3 say park; tip `1bf168f` still added `save_user_profile`, daemon wiring, onboarding rewrite after the park signature. Needs Jacob carve-out or freeze — not an inline doc guess. Points at [`pre-ship-decisions.md`](./pre-ship-decisions.md) row 3.

2. **Skill markdown links to `../../USER.md` / `../../calibration/*`** — 17 occurrences fail exact-path checks from `skills/*/`. Runtime `{user_root}` files; several lack `templates/` seeds. Product judgment whether to retarget links or seed templates.

3. **Remaining AGENTS ↔ SKILL trigger soft drift** — dirs ↔ AGENTS skill ids are **14/14 exact**. Bracketed human-template triggers (e.g. `"how does [prof/course] grade"`) still won’t match natural speech until a plain bullet exists; two were fixed this pass. Broader audit of every AGENTS trigger phrase vs `## Triggers` is judgment / eval work.

4. **`WRITE_SKILL_CATEGORIES` / permissions still model `canvas_submit` / discussion-post as escalatable** while MCP tools have no execute path — vestigial knobs; cleaning needs a permissions-schema decision so agents don’t infer “gated ⇒ can escalate to submit.”

5. **`mark_conversations_read` is a real Canvas PUT** (`messaging.py`) with `destructiveHint=False` and **no ConfirmationGuard** — private inbox state, not submit/comment/discussion-post. Still an ungated Canvas-visible write. Policy call: leave as shared upstream behavior, or gate / preview it. (`mark_module_item_done` is already preview→token gated.)

6. **`env.template` vs script-local keys** — no `.env.example` (by design). Browser/OAuth helpers (`AUTH_DIR`, `HEADLESS`, etc.) still incomplete vs usage. Partial keys (`PRODUCT_USER_ID`, `AUDIT_LOG_DIR`, `SCHOOLS_DIR`) present from prior pass.

7. **Historical handoff docs** still describe pre-addendum Gmail/GCal hard-block in places — architecture is current; rewriting every historical handoff risks erasing timeline. Prefer “superseded” notes on punch-list docs agents still open.

8. **Live MCP due-list escape hatch** — `canvas-week-plan` allows `get_my_upcoming_assignments` when inbox is stale with prose-only writeback. Dual-truth risk vs `week.md` / dock. See Shape S2.

9. **Rust `user_root` ignores `PRODUCT_USER_ID`** — JS/Python honor it; `app/src-tauri/src/inbox.rs` hardcodes `"dev"`. Silent split if multi-profile ever used. See Shape S1.

10. **`cargo test` / Rust daemon growth unverified this pass** — toolchain absent. Highest residual verification gap for tip-commit daemon edits.

11. **GitHub `weekly-maintenance.yml` absent** — workflow-permission tests skip defensively. Dormant assertions, not a wiring bug.

12. **Dead `SWIFT_HELPER` path constant** in apple-cal — cosmetic; leave until EventKit decision (already hard-blocked).

---

## Shape observations

### Ownership (one sentence each)

| Dir | Owns |
|-----|------|
| `schools/` | Declarative tenant identity — only school registry. |
| `browser/` | SSO → `/api/v1` → inbox without PAT — default truth-path fill. |
| `src/canvas_mcp/core/` | Local-first brain (user_root, permissions, ledger, router, learn_loop, guards). |
| `src/canvas_mcp/tools/` + `server.py` | Optional PAT MCP tool surface over Canvas REST. |
| `skills/` | Agent instruction bundles — conversational procedure. |
| `app/` | Tauri tray/dock/daemon shell (signed park; see S3). |
| `plugins/` | School Bucket-A connectors + static registry. |
| `mcp-servers/` | Personal Gmail/GCal/apple-cal actuators + shared write gate. |
| `tools/` | Student MCP inventory (manifest/schemas) — catalog, not runtime. |
| `tests/` | Contract regression for core/security/tools (+ browser tests). |

### S1 — Triple `user_root`; Rust drops `PRODUCT_USER_ID`
- **Observed:** `user_root.py` + `browser/scripts/lib/user-root.mjs` honor `PRODUCT_USER_ID`; Rust `inbox.rs` always joins `"dev"` after `DEV_USER_ROOT`.
- **Why it matters:** Multi-profile / non-dev users can split daemon/dock from sync/Python.
- **Proposal:** Same `PRODUCT_USER_ID || "dev"` rule in Rust; extend parity test.
- **Size/risk:** small.

### S2 — MCP due-list escape hatch is honor-system writeback
- **Observed:** `skills/canvas-week-plan/SKILL.md` allows live `get_my_upcoming_assignments` then “write into inbox.”
- **Why it matters:** Session can answer from MCP without durable `week.md` update.
- **Proposal:** Prefer sync-only in agent sessions; or a tiny merge helper if the escape hatch stays.
- **Size/risk:** small → needs a real plan for merge semantics.
- **External:** Upstream [vishalsachdev/canvas-mcp](https://github.com/vishalsachdev/canvas-mcp) is PAT-first tool surface with no SSO→inbox layer — ProductName’s inbox-as-memory is the differentiator; live-MCP shortcuts erode it.

### S3 — Tauri “parked” vs post-park feature landings
- **Observed:** Signed park 2026-09-12 ([`pre-ship-decisions.md`](./pre-ship-decisions.md) #3); commits after (`5a11923`, `1bf168f`) expanded dock/onboarding/profile. IPC layout itself (`main` → commands/daemon/dock/inbox) is coherent enough not to force a restructure finding.
- **Why it matters:** Agents can’t tell maintain-as-built from expand-features.
- **Proposal:** Amend decision 3 with a carve-out (“learning-surface IPC OK; no shell replace / Hidden-peek”) **or** freeze further `app/` feature work. Do **not** re-litigate shell replacement — already signed park.
- **Size/risk:** trivial (doc) / needs a real plan (if reverting).
- **External:** Upstream canvas-mcp has no desktop shell — this is ProductName-only surface.

### S4 — Vendor docs vs student-only fork
- **Observed:** `tools/README.md` + upstream-shaped root `CHANGELOG` still describe educator / `execute_typescript` surfaces absent from the student registry (`TOOL_MANIFEST` = 41 student tools). `vendor/README.md` boundary is otherwise honest.
- **Why it matters:** Contributors implement ghosts.
- **Proposal:** Trim `tools/README.md` to manifest-aligned student surface; leave upstream CHANGELOG with its banner.
- **Size/risk:** small.
- **External:** Upstream ships ~80–100 tools including educator + code execution; this fork strips them and adds preview-only Canvas-visible writes.

### S5 — Write-gating split — proportionate, no change
- **Observed:** `ConfirmationGuard` + `get_connector_guard` + `gate_connector_write`; Canvas submit/comment/discussion forever preview; personal Gmail/GCal execute only after fingerprint-bound token; apple-cal hard-blocked. `ALWAYS_GATED` clamps `calendar`/`email_send` even if YAML claims automatic.
- **Why it matters:** Sharpest safety shape in the product.
- **Proposal:** **Proportionate, no change.**
- **External:** Upstream canvas-mcp v1.6+ shared write-confirmation (#189) gates Tier-1 student / destructive educator writes with preview→single-use token (and later hardened multi-recipient send previews). ProductName is **stricter on Canvas-visible student actions** (no execute branch at all) and reuses the same guard pattern for personal-account actuators via `gate_connector_write`. Ferro-MCP’s `dispatch_write` confirmation seam is a sibling “confirm then execute once” pattern — maps onto `connector_guards` / `gate_connector_write`, not a reason to invent a fourth gate.

### S6 — Actuator unification — proportionate, no change (until actuator #4)
- **Observed:** gcal/gmail share `mcp-servers/common/actuator.py` for the gate; dry-run/live + undo still live in each `server.py`. apple-cal correctly bypasses the gate as a permanent stub.
- **Proposal:** Extract shared `resolve_live_service` / undo registration only when a fourth live actuator lands.
- **Size/risk:** **proportionate, no change** now.

### S7 — tenant / permissions / ledger for n≈1 — proportionate, no change
- **Observed:** One live school (`cu-boulder`); full posture matrix + ledger.
- **Why it matters:** Looks heavy; protects STOP/rewind and forever-never categories.
- **Proposal:** **Proportionate, no change** until a second school forces tenant pain. Architecture already says second-school support is unproven.

### S8 — Skill-router @ 14 skills — comfortable, no change
- **Observed:** All 14 load dynamically; Triggers + embeddings; no static SKILL_INDEX. Strain would look like frequent ambiguous routes and colliding triggers — not observed after the two trigger bullets above. Soft AGENTS prose vs machine triggers remains a doc hygiene issue, not router overload.
- **Proposal:** **Proportionate, no change.** Optional note that `WRITE_SKILL_CATEGORIES` are refused-posture hooks, not current skill IDs (trivial).

### S9 — `learn_loop.py` size (propose only)
- **Observed:** Largest core module (~1819 lines) owns scheduling, checkpoints, outcomes, due selection, and several render surfaces.
- **Why it matters:** Changes to rendering force reasoning about spacing math in one file.
- **Proposal:** Split into a `learn_loop/` package with re-export shim — **not** done this pass (above trivial-fix bar).
- **Size/risk:** small mechanically, needs a real plan for review.

### S10 — Test suite shape
- **Observed:** Dense unit coverage of brain + write gates; browser 89; foundations via `test_foundations_w0.py`. Thin: some core modules only indirect; Rust/cargo unrun; live SSO/OAuth/NM deferred to human walk; no dedicated `mcp-servers/` test package (actuator covered via `tests/core/`).
- **Proposal:** Don’t expand unit theater; prioritize open human-walk rows 2–4. Optional focused units only if thin modules keep growing.
- **Size/risk:** small / needs a real plan (credentialed).

### S11 — `connector_guards` name vs callers — proportionate, no change
- **Observed:** Production callers today are Gmail/GCal via `actuator.py`; CampusGroups has no MCP write tool yet.
- **Proposal:** Leave as-is; first Bucket-A MCP write must use this path (registry already requires it).

### S12 — Truth-path integrity (browser sync)
- **Observed:** Browser sync uses SSO cookies → Canvas `/api/v1` (no assignment-page DOM scrape found for due lists). Skills may still live-read via PAT MCP when inbox is stale (S2).
- **Proposal:** Keep browser path as primary fill; treat live MCP as refresh-with-writeback only (see S2).
- **Size/risk:** covered by S2 — **proportionate** for the sync layer itself.

---

## Working tree (this pass)

```
 M src/canvas_mcp/core/claim_context.py
 M src/canvas_mcp/core/habit.py
 M src/canvas_mcp/core/teach_hint.py
 M skills/canvas-discussion-facilitator/SKILL.md
 M skills/student-instructor-profile/SKILL.md
?? docs/handoff/audit-2026-09-15.md
?? docs/handoff/audit-prompt-2026-09-15.md   # standing prompt (pre-existing untracked)
```

Branch: `audit/2026-09-15` (off `phase1-productname-pivot`). Nothing committed. No push.
